[project]
name = "olden"
version = "0.1.0"
description = "Heroes of Might and Magic: Olden Era calculators and hero builder"
requires-python = ">=3.10"
dependencies = [
    "django>=5.0",
    "unitypy>=1.24.1",
]
